import styled from "styled-components";
import {ThemedText} from "@Style/Global";

export
const SideName = styled.h1`
  margin: 0;
`
export
const Followers = styled(ThemedText)`
  font-weight: bolder;
`
